# Fix for npm optional dependencies issue with Rollup
fund=false
audit=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Ensure platform-specific binaries are installed
include=optional
