[project]
name = "automation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9,<3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
